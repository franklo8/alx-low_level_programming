#include <stdio.h>
/**
 * main - starting point
 *
 * Description: prints lowercase alphabets
 *
 * Return: 0
 */
int main(void)
{
	char alph;

	for (alph = 'a'; alph <= 'z'; alph++)
	{
		if (alph != 'e' && 'q')
	{
		putchar(alph);
	}
	}
	putchar('\n');
	return (0);
}
