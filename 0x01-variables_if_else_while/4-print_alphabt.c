#include <stdio.h>


/* *
 * main - Starting point
 *
 * Description: 'Prints the alphabet in lowercase'
 *
 * Return: lower case alphabets
 */
int main(void)
{
	char alph;

	for (alph = 'a'; alph <= 'z'; alph++)
	{
		if (alph != 'e' && 'q')
	{
		puchar (alph);
	}
	}
	putchar('\n);
	return (0);
}
